import { describe, it } from 'vitest'
// import { setupNunjucks } from '../../src/serverSetup/nunjucks.js'
// import { runGenericPageTests } from './generic-page.js'
// import config from '../../config/index.js'
// import { testValidationErrorMessage } from './validation-tests.js'

// const nunjucks = setupNunjucks({ datasetNameMapping: new Map() })

describe('dataset task list page', () => {
//   const params = {

  //   }
  //   const html = nunjucks.render('datasetTaskList.html', params)

  //   runGenericPageTests(html, {
  //     pageTitle: 'London Borough of Lambeth — Listed building outline — Task list',
  //     serviceName: config.serviceName
  //   })

  it.todo('it should render the correct headers')

  it.todo('it should render the correct tasks with the correct links and indicator labels')
})
