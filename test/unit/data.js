/**
 * This value should have the same shape as dataSubjects in 'src/utils/utils.js'
 */
export const mockDataSubjects = {
  mockDataset: {
    available: true,
    dataSets: [
      {
        value: 'mockDataset',
        text: 'A Mock dataset',
        available: true
      }
    ]
  }
}
