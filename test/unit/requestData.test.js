import { describe, it, expect } from 'vitest'

// // Tech Debt: we should write some tests around the requestData.js file
describe('RequestData', () => {
  it('need to write these tests', () => {
    expect(true).toBe(true)
  })
})
