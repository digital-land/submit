import { test, expect } from '@playwright/test'

test('the page renders a table with the correct data', async ({ page }) => {
  expect(false).toBe(true)
})

test('the page renders a map with the correct data', async ({ page }) => {
  expect(false).toBe(true)
})

test('clicking the continue button when specifying the data looks correct takes you to the confirmation page', () => {
  expect(false).toBe(true)
})

test('clicking the continue button when specifying the data looks incorrect takes you back to the upload-method page', () => {
  expect(false).toBe(true)
})